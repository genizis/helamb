<a href="{{ "{{ path('" ~ route_name_prefix ~ "_show', { 'id': entity."~ identifier ~" }) }}" }}" class="btn btn-white">
    <i class="fa fa-eye"></i>
</a>

<a href="{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': entity."~ identifier ~" }) }}" }}" class="btn btn-white">
    <i class="fa fa-pencil-square"></i>
</a>

<a href="{{ "{{ path('" ~ route_name_prefix ~ "_delete', { 'id': entity."~ identifier ~" }) }}" }}" class="btn btn-white delete">
    <i class="fa fa-trash-o"></i>
</a>
