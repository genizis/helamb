{% block extends %}
{{ "{% extends '::cms/cms_template.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block crud_block %}" }}
    <h1 class="title">
        <i class="fa fa-tag"></i>
        Adicionar {{ entity }}
    </h1>

    <section class="panel">
	    <div class="panel-body">
	        <div class="position-left">
	            {{ '{{ form(form) }}' }}
	        </div>
	    </div>
	</section>

    {% set hide_edit, hide_add, hide_delete = true, true, true %}
    {% include 'crud/views/others/record_actions.html.twig.twig' %}
{{ "{% endblock %}" }}
{% endblock body %}
